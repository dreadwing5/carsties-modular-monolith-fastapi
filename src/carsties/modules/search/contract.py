"""Public façade of the search module.

Search is a read model fed by auctions' integration events; it currently
exposes nothing to other modules. The file exists so the module boundary rule
("only contract.py may be imported") stays uniform.
"""
